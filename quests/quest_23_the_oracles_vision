#!/usr/bin/python3

#Define a function that takes a length and width and returns the area
def calculate_area(length, width):
    return length * width

# Call the function for two different rectangles and print each result
print("Rectangle 1 area:", calculate_area(4, 8))
print("Rectangle 2 area:", calculate_area(5, 10))


